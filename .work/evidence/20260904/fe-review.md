# 前端（A2 轨 · alukac）实现情况评审（2026-09-04）

> 评审人：后端轨（利用后端 VM 作为差分 oracle 的交叉验证视角）
> 方式：任务清单核对 + 在途 diff 分析 + 测试实测 + **17 项语言特性端到端差分**
> （alukac 编译 → Verifier 校验 → aluvm 执行 vs Go Oracle 源码执行，逐字比对）
> 性质：只读评审——发现的缺陷登记转交 A2 轨，不改前端代码

## 1. 总体结论

**实现质量高，核心链路可用**：任务清单 T-FE-01~17 全部声称完成，实测支撑充分——
26 项测试全绿（parser 6 + compiler 20），17 项特性差分 **13 通过 / 4 缺陷**。
两个此前登记的缺陷之一（`throw` 丢失）**已在本轮在途改动中修复**。
4 项缺陷全部有精确的层次定位与复现，转交清单见 §4。

## 2. 差分矩阵实测（17 项，逐字对拍 Go Oracle）

| 通过（13） | 缺陷（4） |
|---|---|
| 基础函数调用与算术 ✅ | ❌ **递归函数自引用**（`fib(10)` 得 NaN：函数体内自引用编译为 `PUSH_UNDEFINED` callee） |
| try/catch/finally + 异常消息绑定 ✅ | ❌ **子类省略 constructor**：隐式默认构造未转发 `super(...args)`，父类字段未初始化（`this.name` 为 undefined） |
| 闭包与上值计数 ✅ | ❌ **模板字符串插值**：`` `hello ${who} ${1+2}` `` 原样输出——`${}` 插值未解析 |
| 对象字面量/属性读写/索引访问 ✅ | ❌ **对象解构绑定**：`const { k } = { k: 30 }` 的 k 为 undefined（disasm：直接 `PUSH_UNDEFINED` + 残留 `LOAD_GLOBAL k`；数组解构 `[x, y]` 则正确发射 GET_ELEM 序列——**解析层缺口**） |
| 数组 push/length/join/map ✅ | |
| 类定义 + super.speak() 调用链 ✅ | |
| for/while 循环 ✅ | |
| 可选链 + 空值合并 ✅ | |
| switch（含 default）✅ | |
| 后缀/前缀自增自减 ✅ | |
| 箭头函数 + 对象返回 ✅ | |
| 三元 + 逻辑与或 ✅ | |
| **throw 未捕获异常**（修复确认：aluvm 正确展示 `Error: boom`，exit 1）✅ | |

复现脚本与产物：`/tmp/fe_review/m01~m17`（评审会话临时区）。

## 3. 测试与工程现状

- **26 项测试全绿**：aluka-parser 6（词法/解析）、aluka-compiler 9 单元 + 11 集成
  （算术/位运算/比较/作用域/控制流/对象数组/调用/可选链+try/classes/嵌套闭包/端到端）；
- 在途改动规模 +1846/-177（T-FE-01~17 的实现主体，含 codegen 911 行大改）；
- **此前记录的 module.rs 在途编译错误已被 A2 轨自行修复**（评审终验时编译通过）；
- 测试盲区与 §2 缺陷一一对应：现有 11 项集成测试未覆盖递归、throw、模板插值、
  对象解构、默认 constructor——建议修复时同步补测试（防回归）。

## 4. 转交 A2 轨的缺陷清单（按修复优先级）

| # | 缺陷 | 层次 | 建议修复 + 测试 |
|---|---|---|---|
| 1 | 递归函数自引用 → `PUSH_UNDEFINED` callee（顶层函数声明的名字未绑定进函数体作用域） | codegen（函数名符号解析） | 递归 fib/阶乘用例，断言执行结果 |
| 2 | 对象解构绑定模式未解析（`const {k} = obj` 回退错误结构；数组解构已正确） | parser（绑定模式 AST） | 对象/嵌套/默认值解构用例 |
| 3 | 模板字符串插值未解析（含 `${}` 的模板按静态字符串发射） | lexer/parser | 插值/嵌套表达式模板用例 |
| 4 | 子类省略 constructor 时缺隐式 `super(...args)` 转发 | codegen（类构建） | extends 无 constructor 用例 |

**已确认修复**（此前偏差表登记的 throw 缺陷）：本轮在途 codegen 改动已正确发射
THROW，aluvm 未捕获展示链路（`Error: boom` + 退出码）实测通过——该缺陷可关闭。

## 5. 与后端的接口健康度

- alukac 产物全部通过后端 Verifier（V1..V16）严格校验——ISA 契约实现正确；
- aluvm 对 alukac 产物的执行语义在 13 类特性上与 Go Oracle 逐字一致——
  **前后端 ISA 契约的对齐质量高**；4 项缺陷均为前端单侧的发射/解析缺口，
  不涉及 ISA 契约漂移。
